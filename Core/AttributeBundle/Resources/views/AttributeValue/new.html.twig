{% extends '::admin.html.twig' %}
{% block body %}
<h1>{% trans %}Attribute value creation{% endtrans %}</h1>

{{ form_start(form) }}
{% include 'CoreCommonBundle:Form:translations.html.twig' with {'form': form.translations, 'cultures': cultures} %}
{{ form_widget(form.serialized) }}
{{ form_widget(form) }}
<p>
    <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-ok"></span> {%trans%}Create{%endtrans%}</button>
    <a href="{{ path('attributevalue', {'attribute': attribute}) }}" class="btn">
        <span class="glyphicon glyphicon-list-alt"></span> {%trans%}Back to the list{%endtrans%}
    </a>
</p>
{{ form_end(form) }}
{% endblock %}
