{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AttributeValue</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ entity.value }}</td>
            </tr>
            <tr>
                <th>Serialized</th>
                <td>{{ entity.serialized }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('attributevalue') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('attributevalue_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
