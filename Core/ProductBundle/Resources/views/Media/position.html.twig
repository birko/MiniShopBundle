{% extends '::admin.html.twig' %}
{% block body %}
<h1>{% trans %}Product media position edit{% endtrans %}</h1>

<form action="{{ path('product_media_position_update', { 'id': id, 'product': product, 'category': category }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn btn-success"><span class="glyphicon glyphicon-ok"></span> {% trans %}Edit{% endtrans %}</button>
        <a href="{{ path('product_media', {'category': category, 'product': product}) }}"> {% trans %}Back to the list{% endtrans %}</a>
    </p>
</form>
{% endblock %}
