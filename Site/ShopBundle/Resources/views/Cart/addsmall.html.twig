<form action="{{ path('cart_add', {'product': productId})}}" method="post">
    <div>
        <input type="hidden" name="cart-add-small" value="true" />
        <button type="submit" class="btn btn-default btn-block pull-right">
          <span class="glyphicon glyphicon-shopping-cart"></span>
        </button>
    </div>
</form>
